# Datasets

Keep tiny tracked manifests here and place raw benchmark files under `datasets/local/` or point the Optuna driver at absolute dataset paths.

This repo does not commit benchmark corpora by default.
